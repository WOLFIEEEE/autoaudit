# Default audit settings. Override via request body or env vars.
audit:
  level: aa
  wait_ms: 400
  max_tabs: 500
  timeout_seconds: 120
  viewport:
    width: 1280
    height: 720

# robots.txt is ignored by design. See README.
policy:
  ignore_robots_txt: true
